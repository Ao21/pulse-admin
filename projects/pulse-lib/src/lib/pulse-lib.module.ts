import { NgModule } from '@angular/core';
import { PulseLibComponent } from './pulse-lib.component';

@NgModule({
  imports: [
  ],
  declarations: [PulseLibComponent],
  exports: [PulseLibComponent]
})
export class PulseLibModule { }
